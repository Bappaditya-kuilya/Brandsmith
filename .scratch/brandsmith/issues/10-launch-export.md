# 10: Launch Kit + Export

**What to build:** S8 launch assets (hero, pitch, 3 social posts, bio); kit page; export Markdown + JSON + print CSS for PDF; share link read-only, unguessable, 30-day expiry (hash stored).

**Blocked by:** 09.

**Status:** ready-for-agent

- [ ] Four asset types generated and shown on kit page
- [ ] Export MD + JSON downloads work
- [ ] Print stylesheet produces usable PDF via browser print
- [ ] Share URL works read-only without owner cookie; token hashed at rest
